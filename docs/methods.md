# HTTP methods

### GET

```rust
server.get("/", | req, res| {});
```

### POST

```rust
server.post("/", | req, res| {});
```

### PUT

```rust
server.put("/", | req, res| {});
```

### PATCH

```rust
server.patch("/", | req, res| {});
```

### DELETE

```rust
server.delete("/", | req, res| {});
```

